# Vercel Deployment Environment Variables

# Django Settings
DEBUG=False
SECRET_KEY=your-secret-key-here-change-in-production
ENVIRONMENT=production

# Database Configuration (MySQL on Josted)
DB_ENGINE=django.db.backends.mysql
DB_NAME=u386073008_fitness_morocc
DB_USER=u386073008_fitness_admin
DB_PASSWORD=?M5Jh2NWSi
DB_HOST=auth-db1815.hstgr.io
DB_PORT=3306

# Allowed hosts (update with your Vercel domain)
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.vercel.app

# CSRF and Security
CSRF_TRUSTED_ORIGINS=https://your-domain.vercel.app

# Optional: Email Configuration (if using email features)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
